---
name: RawInput
menu: Form
route: /form/raw-input
---

import { Props, Playground } from 'docz';
import { Label } from '../Label';
import { RawInput } from './';
import { Box } from '../../structure/Box';

# RawInput

> 📣 All raw inputs expect to be fully controlled by a wrapper component.

Primarily used for text input or great as a hidden, behind-the-scenes field.

On CoPro we use it to power our autocomplete text fields and tabular data filter text field.

<Playground>
  <Box align="stretch" maxWidth="500px">
    <Label field="foo" title="Text Input" supportingText="intrinsic width = 100% (default)">
      <RawInput
        type="text"
        name="foo"
        placeholder="Your name"
        maxLength={100}
      />
    </Label>
    <Label field="bar" title="Email" supportingText="with validation error, intrinsic width = auto">
      <RawInput
        type="email"
        name="bar"
        onChange={() => {}}
        value="What is email?"
        intrinsicWidth="auto"
        hasError
      />
    </Label>
    <Label field="baz" title="Password">
      <RawInput
        type="password"
        name="password"
        intrinsicWidth="auto"
        value="abcdefghijk"
        onChange={() => {}}
      />
    </Label>
    <Label field="baz" title="Number">
      <RawInput
        type="number"
        name="baz"
        intrinsicWidth="auto"
        min="10"
        max="100"
        value="20"
        onChange={() => {}}
      />
    </Label>
  </Box>
</Playground>

### Props

<Props of={RawInput} />
