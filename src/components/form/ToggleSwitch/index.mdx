---
name: ToggleSwitch
menu: Form
route: /form/toggle-switch
---

import { Props, Playground } from 'docz';
import { Label } from '../Label';
import { ToggleSwitch } from './';
import { Box } from '../../structure/Box';

# ToggleSwitch

📣 Raw input fields are designed to be [controlled](https://reactjs.org/docs/forms.html#controlled-components) by their smarter, form-connected parent.

This component is primarily used for simple text input. It's also great as a hidden field that holds the value of a complex frontend component. On CoPro, we use it to power our autocomplete text fields and tabular data filter fields.

<Playground>
  <Box align="stretch" maxWidth="500px">
    <Label title="States">
      <ToggleSwitch id="foo-unchecked" name="foo" label="unchecked / off" checked={false}  />
      <ToggleSwitch id="foo" name="foo" label="checked / on" checked />
    </Label>
    <Label title="with Error">
      <ToggleSwitch id="foo-unchecked" name="foo" label="unchecked / off" checked={false} hasError  />
      <ToggleSwitch id="foo" name="foo" label="checked / on" checked hasError />
    </Label>
  </Box>
</Playground>

### Props

<Props of={ToggleSwitch} />
