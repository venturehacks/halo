---
name: TextField
menu: Formik
---

import { PropsTable, Playground } from 'docz';
import { Formik, FormikActions, Form, Field } from 'formik';
import { TextField } from './formik';
import { Button, Box } from '~/components'

# TextField

<Formik
  initialValues={{ first_name: '', last_name: '' }}
  onSubmit={((values, actions) => {
    actions.setErrors({
      first_name: 'First name is lame',
      last_name: 'so is your last name :(',
    });
  })}
>
  {(formik) => (
    <Form>
      <Box row>
        <div>
          <TextField field="first_name" placeholder="First name" />
        </div>
        <Box padding>&nbsp;</Box>
        <div>
          <TextField field="last_name" placeholder="Last name" />
        </div>
      </Box>
      <Button type="submit">Submit</Button>
    </Form>
  )}
</Formik>



## Props

⚠️ Disabled for now. Upcoming versions of docz will be able to sort out HoC components.
