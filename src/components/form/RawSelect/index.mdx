---
name: RawSelect
menu: Form
route: /form/raw-select
---

import { Props, Playground } from 'docz';
import { Label } from '../Label';
import { RawSelect } from './';
import { Box } from '../../structure/Box';

# RawSelect

📣 Raw input fields are designed to be [controlled](https://reactjs.org/docs/forms.html#controlled-components) by their smarter, form-connected parent. Be sure to provide an `onChange` handler and manually manage the current selected option via `value` prop.

### Options

```jsx
import { RawSelect, RawSelectOption } from 'halo';

const OPTIONS: RawSelectOption[] = [
  { label: 'Chocolate', value: 'chocolate' },
  { label: 'Vanilla', value: 'vanilla' },
  { label: 'Strawberry', value: 'strawberry' },
];
```

<Playground>
  <Label field="ice-cream" title="One True Ice Cream" supportingText="Select your favorite!" containsFieldGroup>
    <Box row align="space-between" maxWidth="350px">
      <RawSelect
        name="ice-cream"
        value="vanilla"
        options={[
          { label: 'Chocolate', value: 'chocolate' },
          { label: 'Vanilla', value: 'vanilla' },
          { label: 'Strawberry', value: 'strawberry' },
        ]}
        onChange={() => {}}
      />
      <RawSelect
        name="ice-cream"
        value="oops, error"
        options={[
          { label: 'Chocolate', value: 'chocolate' },
          { label: 'Vanilla', value: 'vanilla' },
          { label: 'Strawberry', value: 'strawberry' },
        ]}
        onChange={() => {}}
        hasError
      />
    </Box>
  </Label>
</Playground>

### Props

<Props of={RawSelect} />
