---
name: Span
menu: Core
route: /core/Span
---

import { Props, Playground } from 'docz';
import { Box } from '../../structure/Box';
import { Span } from './'


# Span

The convenient text formatter. Implements common [text interfaces](/constants/text).

### Usage

<Playground>
  <Box row align="space-between" margin={{ bottom: 2 }}>
    <Span>plain</Span>
    <Span italic>italic</Span>
    <Span semibold>semibold</Span>
    <Span bold>bold/strong</Span>
  </Box>

  <Box row align="space-between" background="#f6f6f6" margin={{ bottom: 2}}>
    <Span contrast="AAAA">AAAA contrast</Span>
    <Span contrast="AAA">AAA contrast</Span>
    <Span contrast="AA">AA contrast</Span>
    <Span contrast="A">A contrast</Span>
  </Box>

  <Box row align="space-between" margin={{ bottom: 2 }}>
    <Span muted>muted</Span>
    <Span xmuted>xmuted</Span>
    <Span xxmuted>xxmuted</Span>
  </Box>

  <Box row align="space-between">
    <Span success>success</Span>
    <Span warning>warning</Span>
    <Span error>error</Span>
  </Box>
</Playground>


### Props

<Props of={Span} />


### Do's & Don'ts

✅

```jsx
<Span block error semibold>Quick error message</Span>
<Span size="6xl">{emoji}</Span>
```

❌

```jsx
// no Span-specific feature in use
<Span>hello</Span>

// better off as <span>
<Span className={styles.memo} muted italic>
  already took the time to make .memo, so all styles should live in .memo
</Span>

// lazy style injection
<Header h1>
  <Span color="orange-700" italic>should be a custom class on Header</Span>
</LinkButton>

// lazy style injection
<Anchor onClick={onClick} className={styles.specialLink}>
  <Span color="dark-link" size="sm">
    should be responsibility of Anchor or .specialLink
  </Span>
</Anchor>
```
