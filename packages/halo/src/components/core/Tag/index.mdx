---
name: Tag
menu: Core
route: /core/tag
---

import { Props, Playground } from 'docz';
import { Header } from '../Header';
import { ChatBubbleIcon } from '../../icons';
import { Box } from '../../structure/Box';
import { Tag } from './'


# Tag

Can be used in lieu of a PillTag or Flair

### Usage

<Playground>
  <div>
    <Box margin={{ bottom: 4 }}>
      <Header h5>Small</Header>
      <Tag
        color="red"
        size="sm"
        label="Red Tag"
      />
      <Tag
        color="gray"
        shape="pill"
        size="sm"
        label="Gray Pill Tag"
      />
      <Tag
        icon={<ChatBubbleIcon />}
        shape="pill"
        size="sm"
        label="Blue Pill Tag with icon"
      />
      <Tag
        color="green"
        onClose={() => {alert("close clicked!")}}
        size="sm"
        label="Green Tag with clickable close icon"
      />
      <Tag
        color="purple"
        onClick={() => {alert("tag clicked!")}}
        size="sm"
        label="Clickable tag!"
      />
      <Tag
        color="orange"
        icon={<ChatBubbleIcon />}
        size="sm"
      />
    </Box>

    <Box margin={{ bottom: 4 }}>
      <Header h5>Medium</Header>
      <Tag
        color="red"
        label="Red Tag"
      />
      <Tag
        color="gray"
        shape="pill"
        label="Gray Pill Tag"
      />
      <Tag
        icon={<ChatBubbleIcon />}
        shape="pill"
        label="Blue Pill Tag with icon"
      />
      <Tag
        color="green"
        onClose={() => {alert("close clicked!")}}
        label="Green Tag with clickable close icon"
      />
      <Tag
        color="purple"
        onClick={() => {alert("tag clicked!")}}
        label="Clickable tag!"
      />
      <Tag
        color="orange"
        icon={<ChatBubbleIcon />}
      />
    </Box>

    <Box margin={{ bottom: 4 }}>
      <Header h5>Custom Content</Header>
      <Tag onClose={() => {alert("close clicked!")}} size="sm" >
        <span className="text-xs mr-1">Software Engineer</span>
        <span className="text-2xs font-light">Primary</span>
      </Tag>
      <Tag onClose={() => {alert("close clicked!")}} color="gray" shape="pill" size="md" >
        <span className="text-sm text-dark-aaa mr-1">Software Engineer</span>
        <span className="text-xs text-dark-aaa font-light">Primary</span>
      </Tag>
    </Box>
  </div>
</Playground>

### Exports

```ts
type TagColor =
  | 'blue'
  | 'gray'
  | 'green'
  | 'orange'
  | 'purple'
  | 'red';

type TagShape = 'pill' | 'rectangle';

type TagSize = 'sm' | 'md';
```

### Props

<Props of={Tag} />
