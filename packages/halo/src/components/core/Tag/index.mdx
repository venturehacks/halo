---
name: Tag
menu: Core
route: /core/tag
new: true
figma: https://www.figma.com/file/8EbOKQuVfc61d9aft8p7cO/Halo-Design-Library?node-id=1747%3A6092
---

import { Props, Playground } from 'docz';
import { InterfaceHeader } from '../InterfaceHeader';
import { BookmarkOutlineIcon, CheckIcon } from '../../icons';
import { Tag } from './';

# Tag

Successor to `PillTag` `Flair` (deprecated).

### Usage

<Playground>
  <div className="mb-4">
    <InterfaceHeader title="Non-Interactive" section flow />
    <Tag label="New York" />
    <Tag color="blue" label="Javascript" icon={<CheckIcon/>} />
    <Tag
      icon={<BookmarkOutlineIcon />}
      label="Bookmarked"
    />
    <Tag icon={<BookmarkOutlineIcon />} size="sm" />
  </div>

  <div className="mb-4">
    <InterfaceHeader title="Interactive" section flow />
    <Tag
      onClose={() => {
        alert('close clicked!');
      }}
      label="Remote only"
    />
    <Tag
      color="blue"
      onClose={() => {alert("close clicked!")}}
      onClick={() => {alert("tag clicked!")}}
      label="Paris, France"
    />
    <Tag
     icon={<BookmarkOutlineIcon />}
      onClick={() => {
        alert('tag clicked!');
      }}
      label="Bookmark"
    />
    <Tag color="blue" onClose={() => {alert("close clicked!")}} >
      <span>10 yrs experience</span>
      <span className="text-blue-700">Nice to have</span>
    </Tag>
  </div>
</Playground>

### Size

<Playground>
  <div className="mb-4">
    <Tag label="size xs" />
    <Tag size="sm" label="size sm" />
    <Tag size="md" label="size md" />
  </div>
</Playground>

### Exports

```ts
type TagColor = 'blue' | 'gray';

type TagSize = 'xs' | 'sm' | 'md';
```

### Props

<Props of={Tag} />
