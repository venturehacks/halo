---
name: Tag
menu: Core
route: /core/tag
new: true
---

import { Props, Playground } from 'docz';
import { InterfaceHeader } from '../../structure/InterfaceHeader';
import { BookmarkOutlineIcon } from '../../icons';
import { Box } from '../../structure/Box';
import { Tag } from './'


# Tag

Successor to `PillTag` `Flair` (deprecated)

### Usage

<Playground>
  <Box margin={{ bottom: 4 }}>
    <InterfaceHeader title="2xs" section flow />
    <Tag
      color="red"
      size="sm"
      label="Red tag"
      size="2xs"
    />
    <Tag
      color="gray"
      shape="pill"
      size="sm"
      label="Gray tag"
      size="2xs"
    />
    <Tag
      icon={<BookmarkOutlineIcon />}
      shape="pill"
      size="sm"
      label="Blue tag with icon"
      size="2xs"
    />
    <Tag
      color="green"
      onClose={() => {alert("close clicked!")}}
      size="sm"
      label="Green tag with clickable close icon"
      size="2xs"
    />
    <Tag
      color="purple"
      onClick={() => {alert("tag clicked!")}}
      size="sm"
      label="Clickable tag!"
      size="2xs"
    />
    <Tag
      color="orange"
      icon={<BookmarkOutlineIcon />}
      size="2xs"
    />
  </Box>

  <Box margin={{ bottom: 4 }}>
    <InterfaceHeader title="xs" section flow />
    <Tag
      color="red"
      size="sm"
      label="Red tag"
      size="xs"
    />
    <Tag
      color="gray"
      shape="pill"
      size="sm"
      label="Gray tag"
      size="xs"
    />
    <Tag
      icon={<BookmarkOutlineIcon />}
      shape="pill"
      size="sm"
      label="Blue tag with icon"
      size="xs"
    />
    <Tag
      color="green"
      onClose={() => {alert("close clicked!")}}
      size="sm"
      label="Green tag with clickable close icon"
      size="xs"
    />
    <Tag
      color="purple"
      onClick={() => {alert("tag clicked!")}}
      size="sm"
      label="Clickable tag!"
      size="xs"
    />
    <Tag
      color="orange"
      icon={<BookmarkOutlineIcon />}
      size="xs"
    />
  </Box>

  <Box margin={{ bottom: 4 }}>
    <InterfaceHeader title="sm" section flow />
    <Tag
      color="red"
      label="Red tag"
    />
    <Tag
      color="gray"
      shape="pill"
      label="Gray tag"
    />
    <Tag
      icon={<BookmarkOutlineIcon />}
      shape="pill"
      label="Blue tag with icon"
    />
    <Tag
      color="green"
      onClose={() => {alert("close clicked!")}}
      label="Green tag with clickable close icon"
    />
    <Tag
      color="purple"
      onClick={() => {alert("tag clicked!")}}
      label="Clickable tag!"
    />
    <Tag
      color="orange"
      icon={<BookmarkOutlineIcon />}
    />
  </Box>

  <Box margin={{ bottom: 4 }}>
    <InterfaceHeader title="Arbitrary content" section flow />
    <Tag onClose={() => {alert("close clicked!")}} size="sm" >
      <span className="text-xs font-medium">Software Engineer</span>
      <span className="text-2xs">Primary</span>
    </Tag>
    <Tag
      onClose={() => {alert("close clicked!")}}
      onClick={() => {alert("tag clicked!")}}
      color="gray"
      shape="pill"
      size="md"
    >
      <span className="text-sm text-dark-aaa font-medium">Software Engineer</span>
      <span className="text-xs text-dark-aaa">Primary</span>
    </Tag>
  </Box>
</Playground>

### Exports

```ts
type TagColor =
  | 'blue'
  | 'gray'
  | 'green'
  | 'orange'
  | 'purple'
  | 'red';

type TagShape = 'pill' | 'rectangle';

type TagSize = '2xs' | 'xs' | 'sm' | 'md';
```

### Props

<Props of={Tag} />
