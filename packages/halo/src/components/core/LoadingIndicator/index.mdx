---
name: LoadingIndicator
menu: Core
route: /core/loading-indicator
---

import { Props, Playground } from 'docz';
import { LoadingIndicator } from './';
import { InterfaceHeader } from '../InterfaceHeader';
import { Tooltip } from '../Tooltip';

# LoadingIndicator

Animated component that shows a loading indicator.

### Usage

<Playground>
  <div className="mb-1 flex space-x-8">
    <div>
      <InterfaceHeader
        subsection
        flow
        title="Primary "
        byline="Use with large components and pages"
      />
      <LoadingIndicator bare />
    </div>
    <div>
      <InterfaceHeader
        subsection
        flow
        title="Alternate"
        byline="Use with smaller components"
      />
      <LoadingIndicator bare alternate />
    </div>
  </div>
</Playground>

### Color

While it is possible to alter color and size, Halo defaults are preferred.

<Playground>

<div className="flex space-x-8">
  <div>
    <LoadingIndicator bare color="#666" />
  </div>
  <div>
    <LoadingIndicator bare color="#888" />
  </div>
  <div>
    <LoadingIndicator bare color="#bbb" alternate />
  </div>
  <div>
    <LoadingIndicator bare color="#ddd" alternate />
  </div>
</div>

</Playground>

### Props

<Props of={LoadingIndicator} />
