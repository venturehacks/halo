---
name: SidebarCell
menu: Core
route: /core/sidebar-cell
---

import { Props, Playground } from 'docz';
import { Header } from '../Header';
import { SidebarCell } from './';

# SidebarCell

Display a prominent message and optional action. SidebarCells are displayed at the top of the screen and may be either persistent until dismissed or expired after some time. Avoid stacking more than one SidebarCell.

### Usage

<Playground>
  <div className="max-w-xs bg-blue-100">
    <SidebarCell
      title="Account Executive"
      byline="California, Utah, Colorado, Remote"
      header="Last edit: 7 days ago"
      onClick={() => alert('SidebarCell click!')}
      onStarClick={() => alert('SidebarCell#onStarClick click!')}
      isActive
    />
    <SidebarCell
      title="Head of Product"
      byline="Europe, United States, Remote"
      header="Last edit: 1 month ago"
      onClick={() => alert('SidebarCell click!')}
      onStarClick={() => alert('SidebarCell#onStarClick click!')}
      isStarred
    />
    <SidebarCell
      title="Strategic Project Manager"
      byline="United States, Remote"
      header="Last edit: 2 days ago"
      onClick={() => alert('SidebarCell click!')}
      onStarClick={() => alert('SidebarCell#onStarClick click!')}
    />
    <SidebarCell
      title="Super Long Copywriter Head of Overflow"
      byline="United States, Remote"
      header="Last edit: 2 days ago"
      onClick={() => alert('SidebarCell click!')}
      onStarClick={() => alert('SidebarCell#onStarClick click!')}
    />
  </div>
</Playground>

### Props

<Props of={SidebarCell} />
