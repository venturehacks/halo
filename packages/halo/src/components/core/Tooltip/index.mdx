---
name: Tooltip
menu: Core
route: /core/tooltip
---

import { Props, Playground } from 'docz';
import { Tooltip } from './';

# Tooltip

Our tooltip component is a thin wrapper around Tippy, with small tweaks to styling and prop defaults.

[Tippy.js Prop API](https://atomiks.github.io/tippyjs/all-props/)

### Usage

<Playground>
  <div className="mb-6">
    <Tooltip content="Pop a small tooltip above" size="sm">
      <span>🖲 Hover [sm]</span>
    </Tooltip>
  </div>
  <div className="mb-6">
    <Tooltip content="Pop a medium tooltip above">
      <span>🖲 Hover [md]</span>
    </Tooltip>
  </div>
  <div className="mb-6">
    <Tooltip content="Pop a large tooltip above" size="lg">
      <span>🖲 Hover [lg]</span>
    </Tooltip>
  </div>
  <div className="mb-6">
    <Tooltip placement="right" content="on the right">
      <span>🙌 Customize placement</span>
    </Tooltip>
  </div>
  <Box>
    <Tooltip content="even at an element's corner :)" placement="right-end">
      <img src="https://picsum.photos/id/518/300/200" alt="froot" />
    </Tooltip>
  </Box>
</Playground>

## Apply to a custom component

Likely, you'll want to use a React component as the immediate child to `<Tooltip>`. Be sure to forward the ref!

```tsx
// will fail if you try to wrap in <Tooltip>:
const WillNotWork = () => <button>Custom component</button>;

// Technique 1: use provided higher-order-component
import { withForwardedRef } from 'halo';
const WorksNow = withForwardedRef<Props>(WillNotWork);

// Technique 2: manually forward the ref
const ThisWillWork = React.forwardRef((props, ref) => (
  <button ref={ref}>Text</button>
));
```

### Props

<Props of={Tooltip} />
