---
name: Tooltip
menu: Core
route: /core/tooltip
figma: https://www.figma.com/file/8EbOKQuVfc61d9aft8p7cO/Halo-Design-Library?node-id=210%3A863
---

import { Props, Playground } from 'docz';
import { Tooltip } from './';
import { Tag } from '../Tag';
import { CheckIcon, CompanyIcon, SearchIcon } from '../../icons';

# Tooltip

Our tooltip component is a thin wrapper around Tippy, with small tweaks to styling and prop defaults.

[Tippy.js Prop API](https://atomiks.github.io/tippyjs/all-props/)

### Usage

<Playground>
  <div className="mb-6 flex space-x-8 items-center">
    <Tooltip content="Only show full time positions" size="md">
      <Tag color="blue" size="md" onClose={()=>alert('Clicked')}>Full Time</Tag>
    </Tooltip>

    <Tooltip content="Global Search" size="md" placement="right">
      <div className="w-4 h-4"><SearchIcon /></div>
    </Tooltip>

  </div>
</Playground>

### Size

<Playground>
  <div className="flex space-x-8">
    <div className="mb-6">
      <Tooltip content="Pop a small tooltip above" size="sm">
        <Tag color="blue" size="md">
          Small
        </Tag>
      </Tooltip>
    </div>
    <div className="mb-6">
      <Tooltip content="Pop a medium tooltip above">
        <Tag color="blue" size="md">
          Medium
        </Tag>
      </Tooltip>
    </div>
    <div className="mb-6">
      <Tooltip content="Pop a large tooltip above" size="lg">
        <Tag color="blue" size="md">
          Large
        </Tag>
      </Tooltip>
    </div>
  </div>
</Playground>

### Placement

<Playground>
  <div className="flex space-x-12">
    <div className="mb-6">
      <h4>Right</h4>
      <Tooltip placement="right" content="on the right">
        <div className="h-16 w-16 rounded-md bg-gray-300"></div>
      </Tooltip>
    </div>
    <div className="mb-6">
      <h4>Left</h4>
      <Tooltip placement="left" content="on the left">
        <div className="h-16 w-16 rounded-md bg-green-300"></div>
      </Tooltip>
    </div>
    <div className="mb-6">
      <h4>Top</h4>
      <Tooltip placement="top" content="on the top">
        <div className="h-16 w-16 rounded-md bg-purple-300"></div>
      </Tooltip>
    </div>
    <div className="mb-6">
      <h4>Bottom</h4>
      <Tooltip placement="bottom" content="on the bottom">
        <div className="h-16 w-16 rounded-md bg-orange-300"></div>
      </Tooltip>
    </div>
    <div className="pb-8">
      <h4>Corner</h4>
      <Tooltip content="even at an element's corner :)" placement="right-end">
        <div className="h-16 w-16 rounded-md bg-blue-300"></div>
      </Tooltip>
    </div>
  </div>
</Playground>

## Apply to a custom component

Likely, you'll want to use a React component as the immediate child to `<Tooltip>`. Be sure to forward the ref!

```tsx
// will fail if you try to wrap in <Tooltip>:
const WillNotWork = () => <button>Custom component</button>;

// Technique 1: use provided higher-order-component
import { withForwardedRef } from 'halo';
const WorksNow = withForwardedRef<Props>(WillNotWork);

// Technique 2: manually forward the ref
const ThisWillWork = React.forwardRef((props, ref) => (
  <button ref={ref}>Text</button>
));
```

### Props

<Props of={Tooltip} />

## Video Tutorial

<a id="tutorial">
  <iframe
    width="560"
    height="315"
    src="https://www.youtube.com/embed/nQt8iW_j8d4?start=428"
    title="YouTube video player"
    frameborder="0"
    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
    allowfullscreen
  ></iframe>
</a>
