---
name: Button
menu: Core
route: /core/button
figma: https://www.figma.com/file/8EbOKQuVfc61d9aft8p7cO/Halo-Design-Library?node-id=12%3A1419
video: https://www.youtube.com/embed/nQt8iW_j8d4?start=234
---

import { Props, Playground } from 'docz';
import { Box } from '../../structure/Box';
import { Button } from './';
import { Tooltip } from '../Tooltip';
import { Header } from '../Header';

import { ChevronDownIcon, SearchIcon } from '../../icons';

# Button

Used to trigger an action, such as submit form, open dialog, or cancel action.

### GTM usage

<Playground>
  <div className="flex flex-row gap-5">
    <div className="flex flex-col items-start gap-3">
      <div className="font-medium h-7">
        size
      </div>
      <div className="h-9">
        small
      </div>
      <div className="h-12">
        medium
      </div>
      <div className="h-8">
        large
      </div>
    </div>
    <div className="flex flex-col items-start gap-3">
      <div>
        <code>gtm-primary</code>
      </div>
      <div>
        <Button
          icon={<ChevronDownIcon />}
          iconPosition="right"
          variant="gtm-primary"
          size="sm"
        >
          Create template
        </Button>
      </div>
      <div>
        <Button icon={<SearchIcon />} size="md" variant="gtm-primary">Search</Button>
      </div>
      <div>
        <Button size="lg" variant="gtm-primary" disabled>Apply now</Button>
      </div>
    </div>

    <div className="flex flex-col items-start gap-3">
      <div>
        <code>gtm-secondary</code>
      </div>
      <div>
         <Button variant="gtm-secondary" size="sm">Cancel</Button></div>
      <div>
        <Button variant="gtm-secondary" size="md">Cancel</Button></div>
      <div>
        <Button variant="gtm-secondary" size="lg" disabled>Cancel</Button>
      </div>
    </div>

    <div className="flex flex-col items-start gap-3">
      <div>
        <code>gtm-gray</code>
      </div>
      <div>
        <Button variant="gtm-gray" size="sm">Manage plan</Button></div>
      <div>
        <Button variant="gtm-gray" size="md">Manage plan</Button></div>
      <div>
        <Button variant="gtm-gray" size="lg" disabled>Manage plan</Button>
      </div>
    </div>

    <div className="flex flex-col items-start gap-3">
      <div>
        <code>gtm-error</code>
      </div>
      <div>
        <Button variant="gtm-error" size="sm">Reject</Button></div>
      <div>
        <Button variant="gtm-error" size="md">Reject</Button></div>
      <div>
        <Button variant="gtm-error" size="lg" disabled>Reject</Button>
      </div>
    </div>

    <div className="flex flex-col items-start gap-3">
      <div>
        <code>gtm-clear</code>
      </div>
      <div>
        <Button variant="gtm-clear" size="sm">View more</Button>
      </div>
      <div>
        <Button variant="gtm-clear" size="md">View more</Button>
      </div>
      <div>
        <Button variant="gtm-clear" size="lg" disabled>View more</Button>
      </div>
    </div>

  </div >
</Playground>

### Legacy usage

<Playground>
  <div className="flex flex-row gap-5">
    <div className="flex flex-col items-start gap-3">
      <div>
        <Button icon={<ChevronDownIcon />} iconPosition="right">primary</Button></div>
      <div>
        <Button icon={<SearchIcon />} size="md">primary md</Button></div>
      <div>
        <Button size="lg" disabled>primary lg</Button>
      </div>
    </div>

    <div className="flex flex-col items-start gap-3">
      <div>
        <Button variant="secondary">secondary</Button></div>
      <div>
        <Button variant="secondary" size="md">secondary md</Button></div>
      <div>
        <Button variant="secondary" size="lg" disabled>secondary lg</Button>
      </div>
    </div>

    <div className="flex flex-col items-start gap-3">
      <div>
       <Button variant="gray">gray</Button></div>
      <div>
        <Button variant="gray" size="md">gray md</Button></div>
      <div>
       <Button variant="gray" size="lg" disabled>gray lg</Button></div>
    </div>

    <div className="flex flex-col items-start gap-3">
      <div>
      <Button variant="error">error</Button></div>
      <div>
      <Button variant="error" size="md">error md</Button></div>
      <div>
      <Button variant="error" size="lg" disabled>error lg</Button></div>
    </div>

    <div className="flex flex-col items-start gap-3">
      <div>
        <Button variant="clear">clear</Button></div>
      <div>
        <Button variant="clear" size="md">clear md</Button></div>
      <div>
        <Button variant="clear" size="lg" disabled>clear lg</Button>
      </div>
    </div>

  </div >
</Playground>

### Exports

```ts
type ButtonSize = 'sm' | 'md' | 'lg';
```

```ts
type ButtonVariant =
  | 'primary'
  | 'secondary'
  | 'gray'
  | 'error'
  | 'clear'
  | 'gtm-primary'
  | 'gtm-secondary'
  | 'gtm-gray'
  | 'gtm-clear'
  | 'gtm-error';
```

### Props

<Props of={Button} />
