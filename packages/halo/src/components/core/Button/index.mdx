---
name: Button
menu: Core
route: /core/button
---

import { Props, Playground } from 'docz';
import { Box } from '../../structure/Box';
import { Button } from './'
import { Tooltip } from '../Tooltip'
import { Header } from '../Header'

# Button

### Usage

<Playground>
  <Box row margin={{ bottom: 2 }}>
    <Button>Primary</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="gray">Gray</Button>
    <Button variant="destructive">Destructive</Button>
  </Box>

  <Header h4>Sizes</Header>
  <Box maxWidth="540px" align="space-between" valign="bottom" margin={{ bottom: 2 }} row>
    <Button size="sm">sm</Button>
    <Button size="md" variant="secondary">md</Button>
    <Button size="lg" variant="gray">lg</Button>
  </Box>
</Playground>

### Exports

```ts
type ButtonSize =
  | 'sm'
  | 'md'
  | 'lg'
  | 'large' // deprecated in 0.15
  | 'regular' // deprecated in 0.15
  | 'small' // deprecated in 0.15
  | 'xsmall' // deprecated in 0.15
  | 'inline'; // deprecated in 0.9
```
```ts
type ButtonVariant =
  | 'primary' 
  | 'secondary'
  | 'gray'
  | 'desctructive'
  | 'warning' // deprecated in 0.15
  | 'danger' // deprecated in 0.15
  | 'success' // deprecated in 0.15
  | 'secondary-gray' // deprecated in 0.15
  | 'clear' // deprecated in 0.15
  | 'alternate'; // deprecated in 0.9
```

### Props

<Props of={Button} />
