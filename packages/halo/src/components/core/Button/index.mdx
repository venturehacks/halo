---
name: Button
menu: Core
route: /core/button
---

import { Props, Playground } from 'docz';
import { Box } from '../../structure/Box';
import { Button } from './'
import { Tooltip } from '../Tooltip'
import { Header } from '../Header'

# Button

### Usage

<Playground>
  <div className="mb-6">
    <Button>Primary</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="gray">Gray</Button>
    <Button variant="error">Error</Button>
    <Button variant="clear">Clear</Button>
  </div>

  <div className="mb-6">
    <Button disabled>Primary</Button>
    <Button variant="secondary" disabled>Secondary</Button>
    <Button variant="gray" disabled>Gray</Button>
    <Button variant="error" disabled>Error</Button>
    <Button variant="clear" disabled>Clear</Button>
  </div>

  <Button size="lg" variant="gray">lg</Button>
  <Button size="md" variant="secondary">md</Button>
  <Button size="sm">sm</Button>
</Playground>

### Exports

```ts
type ButtonSize =
  | 'sm'
  | 'md'
  | 'lg'
```
```ts
type ButtonVariant =
  | 'primary'
  | 'secondary'
  | 'gray'
  | 'error'
  | 'success'
  | 'clear';
```

### Props

<Props of={Button} />
